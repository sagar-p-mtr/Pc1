8) Program 8 

Write a MPI Program demonstration of MPI_Scatter and MPI_Gather.



#include <mpi.h>
#include <stdio.h>

int main(int argc, char** argv) {
    int rank, size;
    int send_data[100], recv_data, gather_data[100];
    MPI_Init(&argc, &argv);
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);
    MPI_Comm_size(MPI_COMM_WORLD, &size);

    if (rank == 0) {
        for (int i = 0; i < size; i++) send_data[i] = (i + 1) * 10;
        printf("\nRoot process initialized data: ");
        for (int i = 0; i < size; i++) printf("%d ", send_data[i]);
        printf("\n");
    }

    MPI_Scatter(send_data, 1, MPI_INT, &recv_data, 1, MPI_INT, 0, MPI_COMM_WORLD);
    printf("\nProcess %d received value %d\n", rank, recv_data);

    recv_data *= 2;

    MPI_Gather(&recv_data, 1, MPI_INT, gather_data, 1, MPI_INT, 0, MPI_COMM_WORLD);

    if (rank == 0) {
        printf("\nRoot process gathered modified data: ");
        for (int i = 0; i < size; i++) printf("%d ", gather_data[i]);
        printf("\n");
    }

    MPI_Finalize();
    return 0;
}


mpicc prog8.c -o prog8
mpirun -np 2 ./prog8


